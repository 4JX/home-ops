# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/core/namespace_v1.json
apiVersion: v1
kind: Namespace
metadata:
  name: media
  annotations:
    # Keep the namespace that owns the claim for the pre-existing HDD tree.
    kustomize.toolkit.fluxcd.io/prune: disabled
