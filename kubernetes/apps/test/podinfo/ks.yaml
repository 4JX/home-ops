# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.toolkit.fluxcd.io/kustomization_v1.json
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: podinfo
spec:
  commonMetadata:
    labels:
      app.kubernetes.io/name: podinfo
  dependsOn:
    # The HTTPRoute needs an accepted Gateway and listener to attach to.
    - name: envoy-gateway-gateway
      namespace: network
  interval: 1h
  path: ./kubernetes/apps/test/podinfo/app
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  targetNamespace: test
  timeout: 5m
  # Make the test fail visibly unless the workload and route become healthy.
  wait: true
