# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/gateway.networking.k8s.io/gateway_v1.json
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: envoy
spec:
  # Select the GatewayClass reconciled by the Envoy Gateway controller.
  # https://gateway.envoyproxy.io/docs/tasks/traffic/gatewayapi-support/
  gatewayClassName: envoy
  listeners:
    # The listener name is referenced by Podinfo's HTTPRoute sectionName.
    # https://gateway.envoyproxy.io/v1.8/tasks/traffic/http-routing/
    - name: http
      protocol: HTTP
      # Plain HTTP is sufficient for the first LAN test; TLS comes later.
      port: 80
      allowedRoutes:
        namespaces:
          # Podinfo lives in test while this Gateway lives in network.
          # https://gateway-api.sigs.k8s.io/guides/multiple-ns/
          from: All
