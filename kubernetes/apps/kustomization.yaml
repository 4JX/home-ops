# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/kustomize.config.k8s.io/kustomization_v1beta1.json
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Ordering is expressed by the Flux Kustomization dependsOn graph inside
  # these trees; this root only discovers each cluster concern.
  - ./flux-system
  - ./kube-system
  - ./media
  - ./network
  - ./test
