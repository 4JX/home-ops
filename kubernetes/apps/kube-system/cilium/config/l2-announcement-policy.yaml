# yaml-language-server: $schema=https://k8s-schemas.home-operations.com/cilium.io/ciliuml2announcementpolicy_v2alpha1.json
apiVersion: cilium.io/v2alpha1
kind: CiliumL2AnnouncementPolicy
metadata:
  name: lan
spec:
  # Announce allocated LoadBalancer IPs, not arbitrary externalIPs.
  # https://docs.cilium.io/en/stable/network/l2-announcements/
  loadBalancerIPs: true
